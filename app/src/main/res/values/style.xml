<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BottomSheetTheme" parent="Theme.Design.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="NextButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/next_button_bg</item>
        <item name="android:text">@string/next</item>
        <item name="android:textColor">@color/blue</item>
        <item name="android:textSize">@dimen/next_button_text_size</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_height">@dimen/next_button_height</item>
    </style>

    <style name="AddTitleText" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">@dimen/add_title_text_width</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/add_title_text</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="RectangleEditText" parent="Widget.AppCompat.EditText">
        <item name="android:background">@drawable/edittext_border</item>
        <item name="android:paddingHorizontal">20dp</item>
        <item name="android:layout_height">70dp</item>
    </style>
</resources>
